//STEP 1

/*
let num = prompt('Enter a number');
console.log(`Its absolute value is ${Math.abs(num)}.`);
*/

//STEP 2

/*
let num = prompt('Enter a decimal number');
console.log(`It rounds up to ${Math.ceil(num)}.`);
*/

//STEP 3

/*
let num = prompt('Enter a decimal number');
console.log(`It rounds down to ${Math.floor(num)}.`);
*/

//STEP 4

/*
let numList = prompt('Enter a comma-separated list of 5 numbers (ex: 1,2,3,4,5)');
let numArr = numList.split(',');
console.log(`The largest value is ${Math.max(...numArr)} and the smallest value is ${Math.min(...numArr)}.`);
*/

//STEP 5

/*
let num = prompt('Enter a number');
console.log(`Its square root is ${Math.sqrt(num)}.`);
*/
